{
  "name": "publisher",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/freegamestore-online/publisher"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "cd web && npx wrangler pages dev -- pnpm dev",
    "build": "cd web && pnpm build",
    "test": "cd web && pnpm test"
  }
}
